<button class="bg-red-700">hello</button>

<!-- select(baseSvg).call(drag().on("drag", (e) => {
    const rotate = projection.rotate()
    const k = 75 / projection.scale()
    projection.rotate([
        rotate[0] + e.dx * k,
        rotate[1] - e.dy * k
    ])
    geoGenerator = geoPath().projection(projection);
})); -->